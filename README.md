# Real-Estate-Analytics
A metadata-driven Azure Data Engineering ETL pipeline that ingests real estate CRM CSV files into Blob Storage, validates them in ADF, loads staging tables in SQL Database, and transforms data into final dimension and fact tables using stored procedures.
